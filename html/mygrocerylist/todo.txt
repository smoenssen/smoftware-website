set max-width to 500px for all forms.

Don't allow duplicate entries for list, category, and groceryitem.

When creating new user add default data.

Add support for item quantity.

Where to unset variables

Is IsSelected necessary in GroceryItem table? Adding or removing items from ListCategoryGroceryItem table might be enough.


Bugs
------------------
When adding new item from list purchased items are cleared
